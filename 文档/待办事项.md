使用lombok中的@Data优化DTO层和entity层中的get和set方法
- 启动前端：在 frontend 目录执行 npm run dev ，访问 http://localhost:10001/
- 启动后端：在项目根目录执行 mvn spring-boot:run ，接口前缀 http://localhost:10002/api
- 登录后本地存储会写入 token，Axios 自动携带 `Authorization` 请求头，购物车、订单等接口可直接调用


- 在 UserCenterView.vue 中使用 getMe() 获取当前用户详情
- 完善商品、分类管理页以对接新增 API 模块
- 增加全局错误提示（Element Plus ElMessage ）与表单校验规则
- 已接入 JWT，并在后端用拦截器统一解析用户信息，去除对 X-User-Id 的依赖

- 管理端支持订单详情与发货流程（联动 ShipmentController ）
- 管理端商品图片上传与图集管理（联动 GET /products/{id}/images 与新的上传端点）
- （已完成）接入 JWT 与后端拦截器统一解析用户信息，去除对 X-User-Id 的依赖
                                                    
优化建议：
- 路由基于角色的访问控制已启用，后续在后端增加基于角色的权限校验（注解或拦截器）
- 增加全局异常处理，统一错误结构 `{ code, message, details }`
- 前端引入 Pinia 管理用户与购物车状态，提升可维护性
- 支持用户资料完善与修改，包括手机号、头像与密码变更
- 认证增强：增加刷新令牌与令牌失效处理，提升安全性与体验
